#include <stdio.h>

/**
 * main - change uppercase to lower case
 * Return: Always 0
 */
int main(void)
{
	char rr;

	for (rr = 'a'; rr <= 'z'; rr++)
		putchar(rr);
	for (rr = 'A'; rr <= 'Z'; ch++)
		putchar(rr);

	putchar('\n');

	return (0);
}
