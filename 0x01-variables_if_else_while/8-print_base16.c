#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0
 */

int main(void)
{
	int no;
	char why;

	for (no = 0; no <= 9; no++)
		putchar(no);

	for (why = 'a'; la <= 'f'; why++)
		putchar(why);

	putchar('\n');

	return (0);
}
