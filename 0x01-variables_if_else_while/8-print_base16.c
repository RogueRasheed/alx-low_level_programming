#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - print the numbers in base 16
 * Return: Always 0
 */
int main(void)
{
	int no;
	char why;

	for (no = 0; no <= 10; no++)
		putchar(no);

	for (why = 'a'; la <= 'f'; why++)
		putchar(why);

	putchar('\n');

	return (0);
}
